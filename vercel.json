{
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm build",
  "rewrites": [
    {
      "source": "/:path*",
      "has": [{ "type": "host", "value": "monitor.colourking.nl" }],
      "destination": "/monitor/:path*"
    }
  ]
}
